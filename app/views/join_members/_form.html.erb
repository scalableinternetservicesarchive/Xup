<%= simple_form_for(@join_member) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :party %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
